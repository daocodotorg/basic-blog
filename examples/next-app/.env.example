# Same Convex deployment as `npx convex dev` / `pnpm convex:dev` in this directory.
# After linking a project, your dashboard URL looks like https://happy-animal-123.convex.cloud
NEXT_PUBLIC_CONVEX_URL=
